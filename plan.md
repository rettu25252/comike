## Plan: 買い物リストサイトの実装手順

TL;DR: 既存の空作業フォルダ内で、PWで保護された買い物リストWebサイトを、静的なHTML/CSS/JavaScript構成で実装します。データはブラウザのローカル保存を使い、ページ遷移と編集操作がすべて同じ作業フォルダ内で完結するようにします。

### 1. 事前方針
- 実装方式は静的サイトとして進める。
- 画面遷移は単一ページ内の状態管理でも、複数ページの切り替えでも実現可能だが、作業を分かりやすくするために「4つの画面」を分けて構成する。
- PWはブラウザに保存し、正しいPWを入力した場合のみ各画面へ進める。
- リストとアイテムのデータはローカルストレージに保存する。

### 2. 画面構成
1. PW入力ページ
   - 初回アクセス時にPWを設定できる。
   - 設定済みのPWを入力すると、リスト選択ページへ移る。
2. リスト選択ページ
   - 既存の買い物リストを一覧表示する。
   - 新規作成ボタンで新しいリストを作成できる。
   - 既存リストを選択すると閲覧ページへ移る。
3. リスト閲覧ページ
   - 選択中のリスト内のアイテムを一覧表示する。
   - 各アイテムの名前、価格、メモ、購入済み状態を確認できる。
   - 購入済みチェックはこの画面で更新できる。
   - 編集ページへ移動できる。
4. リスト編集ページ
   - リスト名の変更、アイテムの追加・編集・削除ができる。
   - 価格・メモ・購入済み状態もここで更新できる。

### 3. データ構造
- リスト: id, name
- アイテム: id, name, price, memo, purchased
- アプリ全体の状態: password, lists, currentListId

### 4. 実装ステップ
1. 画面用のHTMLを作成する。
   - PW入力画面
   - リスト選択画面
   - 閲覧画面
   - 編集画面
2. デザイン用のCSSを作成し、画面ごとに見やすいレイアウトにする。
3. JavaScriptで画面遷移と状態管理を実装する。
4. PW設定・入力・確認ロジックを実装する。
5. リスト作成・選択・一覧表示を実装する。
6. リスト閲覧画面で購入済みチェックを切り替えられるようにする。
7. 編集画面でアイテムの追加・編集・削除ができるようにする。
8. ローカルストレージへの保存・復元を実装する。
9. 動作確認を行い、画面遷移やデータ保存に不具合がないか確認する。

### 5. 実装対象ファイル
- index.html
- styles.css
- app.js
- 必要に応じて README.md

### 6. 検証項目
- PWを設定して正しく入力すると次の画面へ進むこと
- リストを作成して選択できること
- アイテムの追加・編集・削除ができること
- 購入済み状態が保存されること
- ページを再読み込みしてもデータが保持されること

### 7. 追加方針
- まずは最小機能で動作する形を作り、必要に応じてUIを改善する。
- 実装後は、必要なら見た目をより使いやすいものに調整する。
